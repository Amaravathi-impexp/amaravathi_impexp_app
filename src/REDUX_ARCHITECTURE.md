# 🏗️ Redux Architecture Diagram

## Overview

This document provides a visual representation of the Redux architecture in the Amaravathi Imports & Exports application.

---

## 🔄 Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         REACT COMPONENTS                         │
│  (SignIn, Dashboard, Settings, Shipments, Partners, etc.)       │
└────────────────────┬─────────────────────┬──────────────────────┘
                     │                     │
                     │ useAppDispatch()    │ useAppSelector()
                     │                     │
                     ▼                     ▼
         ┌───────────────────┐   ┌──────────────────┐
         │   DISPATCH        │   │   SELECT STATE   │
         │   (Actions)       │   │   (Selectors)    │
         └─────────┬─────────┘   └────────┬─────────┘
                   │                      │
                   │                      │
                   ▼                      │
         ┌─────────────────────────────┐ │
         │      REDUX STORE            │ │
         │  ┌─────────────────────┐    │ │
         │  │   Auth Slice        │◄───┼─┘
         │  │  - user             │    │
         │  │  - token            │    │
         │  │  - isAuthenticated  │    │
         │  └─────────────────────┘    │
         │  ┌─────────────────────┐    │
         │  │  Shipments Slice    │    │
         │  │  - shipments[]      │    │
         │  │  - selected         │    │
         │  │  - loading          │    │
         │  └─────────────────────┘    │
         │  ┌─────────────────────┐    │
         │  │   Partners Slice    │    │
         │  │  - partners[]       │    │
         │  │  - selected         │    │
         │  └─────────────────────┘    │
         │  ┌─────────────────────┐    │
         │  │    Users Slice      │    │
         │  │  - users[]          │    │
         │  │  - selected         │    │
         │  └─────────────────────┘    │
         │  ┌─────────────────────┐    │
         │  │     UI Slice        │    │
         │  │  - currentView      │    │
         │  │  - notifications[]  │    │
         │  │  - sidebar          │    │
         │  └─────────────────────┘    │
         └─────────────────────────────┘
```

---

## 📦 Store Structure

```
REDUX STORE
│
├── auth/
│   ├── user: User | null
│   ├── token: string | null
│   └── isAuthenticated: boolean
│
├── shipments/
│   ├── shipments: Shipment[]
│   ├── selectedShipment: Shipment | null
│   ├── loading: boolean
│   └── error: string | null
│
├── partners/
│   ├── partners: Partner[]
│   ├── selectedPartner: Partner | null
│   ├── loading: boolean
│   └── error: string | null
│
├── users/
│   ├── users: User[]
│   ├── selectedUser: User | null
│   ├── loading: boolean
│   └── error: string | null
│
└── ui/
    ├── currentView: ViewType
    ├── notifications: Notification[]
    ├── sidebarCollapsed: boolean
    └── theme: 'light' | 'dark'
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│  SignIn.tsx  │
└──────┬───────┘
       │
       │ 1. User enters credentials
       ▼
┌─────────────────────┐
│  mockApi.auth.login │
│  { email, password }│
└──────────┬──────────┘
           │
           │ 2. API returns { user, token }
           ▼
┌───────────────────────────────┐
│  dispatch(setCredentials({    │
│    user: response.user,       │
│    token: response.token      │
│  }))                          │
└──────────┬────────────────────┘
           │
           │ 3. Redux updates auth state
           ▼
┌─────────────────────────────┐
│  Redux Store Updated:       │
│  auth.user = {...}          │
│  auth.token = "abc123..."   │
│  auth.isAuthenticated = true│
└──────────┬──────────────────┘
           │
           │ 4. Components re-render
           ▼
┌─────────────────────────┐
│  Dashboard.tsx          │
│  const user =           │
│    useAppSelector(...)  │
└─────────────────────────┘
```

---

## 🚢 Shipment Creation Flow

```
┌──────────────────────┐
│ CreateShipment.tsx   │
│ (5-step wizard)      │
└──────────┬───────────┘
           │
           │ 1. User completes form
           ▼
┌───────────────────────────┐
│ mockApi.shipments.create  │
│ (shipmentData)            │
└──────────┬────────────────┘
           │
           │ 2. API returns new shipment
           ▼
┌────────────────────────────┐
│ dispatch(addShipment(      │
│   newShipment              │
│ ))                         │
└──────────┬─────────────────┘
           │
           │ 3. Redux updates shipments array
           ▼
┌──────────────────────────────┐
│ Redux Store Updated:         │
│ shipments.shipments = [      │
│   newShipment,               │
│   ...existing                │
│ ]                            │
└──────────┬───────────────────┘
           │
           │ 4. Notification added
           ▼
┌────────────────────────────────┐
│ dispatch(addNotification({     │
│   type: 'success',             │
│   message: 'Shipment created!' │
│ }))                            │
└────────────────────────────────┘
```

---

## 🎨 Navigation Flow

```
┌──────────────┐
│ Navigation   │
│ Component    │
└──────┬───────┘
       │
       │ User clicks "Sign In"
       ▼
┌──────────────────────────┐
│ dispatch(setCurrentView( │
│   'signin'               │
│ ))                       │
└──────────┬───────────────┘
           │
           │ Redux updates UI state
           ▼
┌────────────────────────┐
│ ui.currentView = 'signin'│
└──────────┬───────────────┘
           │
           │ App.tsx re-renders
           ▼
┌────────────────────────┐
│ const currentView =    │
│   useAppSelector(...)  │
│                        │
│ if (currentView ===    │
│     'signin') {        │
│   return <SignIn />    │
│ }                      │
└────────────────────────┘
```

---

## 🔄 State Update Pattern

```
BEFORE (localStorage)
─────────────────────

Component
    │
    ├─ localStorage.getItem('user')
    ├─ JSON.parse()
    ├─ Update value
    ├─ JSON.stringify()
    └─ localStorage.setItem('user', ...)


AFTER (Redux)
────────────

Component
    │
    ├─ useAppSelector(state => state.auth.user)  // Read
    │     ▼
    │   Redux Store (instant, type-safe)
    │
    └─ dispatch(updateUser({ ...user, ... }))    // Write
          ▼
        Redux Store (automatic update)
          ▼
        All subscribed components re-render
```

---

## 🏗️ Component Integration

```
┌─────────────────────────────────────────────────────────┐
│                      App.tsx                            │
│  <Provider store={store}>                               │
│    <AppContent />                                       │
│  </Provider>                                            │
└────────────────────────┬────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┬─────────────┐
         │               │               │             │
         ▼               ▼               ▼             ▼
    ┌─────────┐   ┌──────────┐   ┌──────────┐  ┌──────────┐
    │ SignIn  │   │Dashboard │   │ Settings │  │ Shipments│
    └────┬────┘   └────┬─────┘   └────┬─────┘  └────┬─────┘
         │             │              │             │
         │             │              │             │
    ┌────▼─────────────▼──────────────▼─────────────▼─────┐
    │              useAppDispatch()                        │
    │              useAppSelector()                        │
    │                                                      │
    │              Access Redux Store                      │
    └──────────────────────────────────────────────────────┘
```

---

## 📊 Slice Actions & Reducers

```
┌──────────────────────────────────────────────────────┐
│              authSlice.ts                            │
├──────────────────────────────────────────────────────┤
│  STATE:                                              │
│    - user: User | null                               │
│    - token: string | null                            │
│    - isAuthenticated: boolean                        │
├──────────────────────────────────────────────────────┤
│  ACTIONS:                                            │
│    - setCredentials(user, token)                     │
│      → Updates user and token                        │
│      → Sets isAuthenticated = true                   │
│                                                      │
│    - updateUser(user)                                │
│      → Updates user data                             │
│                                                      │
│    - logout()                                        │
│      → Clears user and token                         │
│      → Sets isAuthenticated = false                  │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│            shipmentsSlice.ts                         │
├──────────────────────────────────────────────────────┤
│  STATE:                                              │
│    - shipments: Shipment[]                           │
│    - selectedShipment: Shipment | null               │
│    - loading: boolean                                │
│    - error: string | null                            │
├──────────────────────────────────────────────────────┤
│  ACTIONS:                                            │
│    - setShipments(shipments[])                       │
│    - addShipment(shipment)                           │
│    - updateShipment(shipment)                        │
│    - deleteShipment(id)                              │
│    - setSelectedShipment(shipment)                   │
│    - setLoading(boolean)                             │
│    - setError(message)                               │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│               uiSlice.ts                             │
├──────────────────────────────────────────────────────┤
│  STATE:                                              │
│    - currentView: ViewType                           │
│    - notifications: Notification[]                   │
│    - sidebarCollapsed: boolean                       │
│    - theme: 'light' | 'dark'                         │
├──────────────────────────────────────────────────────┤
│  ACTIONS:                                            │
│    - setCurrentView(view)                            │
│    - addNotification({ type, message })              │
│    - removeNotification(id)                          │
│    - clearNotifications()                            │
│    - toggleSidebar()                                 │
│    - setSidebarCollapsed(boolean)                    │
│    - setTheme(theme)                                 │
└──────────────────────────────────────────────────────┘
```

---

## 🔧 API Integration

```
┌─────────────────────────────────────────────────────┐
│              Component (e.g., SignIn)               │
└──────────────────┬──────────────────────────────────┘
                   │
                   │ 1. User action
                   ▼
         ┌──────────────────────┐
         │   mockApi.auth.login │
         │   (email, password)  │
         └──────────┬───────────┘
                    │
                    │ 2. HTTP request
                    ▼
         ┌──────────────────────┐
         │   /services/api.ts   │
         │   getAuthToken()     │
         └──────────┬───────────┘
                    │
                    │ 3. Get token from Redux
                    ▼
         ┌──────────────────────┐
         │   store.getState()   │
         │   .auth.token        │
         └──────────┬───────────┘
                    │
                    │ 4. Add to request headers
                    ▼
         ┌──────────────────────────┐
         │  fetch(url, {            │
         │    headers: {            │
         │      Authorization:      │
         │        `Bearer ${token}` │
         │    }                     │
         │  })                      │
         └──────────┬───────────────┘
                    │
                    │ 5. Response
                    ▼
         ┌──────────────────────┐
         │   Component receives │
         │   response data      │
         └──────────┬───────────┘
                    │
                    │ 6. Dispatch to Redux
                    ▼
         ┌──────────────────────┐
         │ dispatch(            │
         │   setCredentials()   │
         │ )                    │
         └──────────────────────┘
```

---

## 🎯 Benefits Visualization

```
┌─────────────────────────┐      ┌─────────────────────────┐
│    localStorage         │      │        Redux            │
├─────────────────────────┤      ├─────────────────────────┤
│                         │      │                         │
│ ❌ Manual JSON parse    │  →   │ ✅ Direct access        │
│ ❌ No type safety       │  →   │ ✅ TypeScript types     │
│ ❌ Scattered state      │  →   │ ✅ Centralized          │
│ ❌ Hard to debug        │  →   │ ✅ DevTools             │
│ ❌ Sync issues          │  →   │ ✅ Automatic sync       │
│ ❌ Browser storage      │  →   │ ✅ In-memory            │
│ ❌ Security concerns    │  →   │ ✅ Session-based        │
│                         │      │                         │
└─────────────────────────┘      └─────────────────────────┘
```

---

## 📈 Performance Comparison

```
STATE ACCESS PERFORMANCE
────────────────────────

localStorage:
  User clicks → Component reads localStorage → Parse JSON → Update state → Re-render
  Time: ~5-10ms per access

Redux:
  User clicks → Component reads Redux → Direct reference → Re-render
  Time: ~1-2ms per access

STATE UPDATE PERFORMANCE
───────────────────────

localStorage:
  Update → Stringify JSON → Write to disk → Read → Parse → Update state → Re-render
  Time: ~10-20ms per update

Redux:
  Update → Dispatch action → Reducer updates → Subscribed components re-render
  Time: ~2-5ms per update
```

---

## 🎨 User Experience Flow

```
LOGIN FLOW
──────────

User enters credentials
        │
        ▼
   ┌─────────┐
   │ SignIn  │
   └────┬────┘
        │
        ▼
  [Validate Input]
        │
        ▼
  [Call API]
        │
        ▼
  [Success?] ──No──> [Show Error]
        │
       Yes
        │
        ▼
  [Dispatch setCredentials]
        │
        ▼
  [Redux Updates]
        │
        ▼
  [Navigate to Dashboard]
        │
        ▼
  [Dashboard Loads User from Redux]
        │
        ▼
  [Show User Data]


LOGOUT FLOW
───────────

User clicks logout
        │
        ▼
  [Dispatch logout()]
        │
        ▼
  [Redux Clears State]
        │
        ▼
  [Dispatch setCurrentView('home')]
        │
        ▼
  [Navigate to Home]
        │
        ▼
  [User logged out]
```

---

## 🏁 Summary

The Redux architecture provides:
- ✅ Centralized state management
- ✅ Type-safe operations
- ✅ Better debugging
- ✅ Improved performance
- ✅ Scalable structure
- ✅ Session-based security

All components have access to the same Redux store, ensuring consistency and eliminating synchronization issues!
