# 🏗️ RTK Query Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         React Components                            │
│  (Dashboard, Shipments, Partners, Users, etc.)                      │
└──────────────────────┬──────────────────────────────────────────────┘
                       │
                       │ Import & Use Hooks
                       │
       ┌───────────────┼───────────────┬─────────────────────┐
       │               │               │                     │
       ▼               ▼               ▼                     ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────────┐
│   Queries   │ │  Mutations  │ │ Lazy Queries│ │  Manual Cache   │
│             │ │             │ │             │ │   Management    │
│ useGetXXX   │ │ useCreateXXX│ │useLazyGetXXX│ │ invalidateTags  │
│ Query()     │ │ Mutation()  │ │ Query()     │ │ resetApiState   │
└──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └────────┬────────┘
       │               │               │                  │
       │               │               │                  │
       └───────────────┴───────────────┴──────────────────┘
                       │
                       │ All routes through
                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      RTK Query API Slices                           │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐               │
│  │   authApi    │ │ shipmentsApi │ │  partnersApi │               │
│  │              │ │              │ │              │               │
│  │ • login      │ │ • getAll     │ │ • getAll     │               │
│  │ • signUp     │ │ • getById    │ │ • getById    │               │
│  │ • logout     │ │ • create     │ │ • create     │               │
│  │ • refresh    │ │ • update     │ │ • update     │               │
│  └──────────────┘ │ • delete     │ │ • delete     │               │
│                   │ • track      │ └──────────────┘               │
│  ┌──────────────┐ └──────────────┘                                │
│  │   usersApi   │ ┌──────────────┐                                │
│  │              │ │ dashboardApi │                                │
│  │ • getAll     │ │              │                                │
│  │ • getById    │ │ • getStats   │                                │
│  │ • create     │ │ • getRevenue │                                │
│  │ • update     │ │ • getActivities                               │
│  │ • delete     │ └──────────────┘                                │
│  └──────────────┘                                                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ All inherit from
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        Base API (baseApi.ts)                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  • fetchBaseQuery configuration                             │   │
│  │  • Base URL: https://api.amaravathi.com/v1                  │   │
│  │  • prepareHeaders() - Auto token injection                  │   │
│  │  • baseQueryWithReauth() - Handle 401 errors                │   │
│  │  • Tag Types: Auth, Shipments, Partners, Users, etc.        │   │
│  └─────────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ Uses
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Native Fetch API (Browser)                       │
│  • GET, POST, PUT, PATCH, DELETE requests                           │
│  • Automatic request/response handling                              │
│  • Headers management                                               │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Backend API Server                          │
│                  (Mock or Real REST API)                            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow

### Query Flow (GET Requests)

```
1. Component calls hook
   ↓
   const { data, isLoading } = useGetShipmentsQuery({ page: 1 });
   
2. RTK Query checks cache
   ↓
   ┌─────────────────┐
   │ Cache Hit?      │
   └────┬────────┬───┘
        │        │
    YES │        │ NO
        │        │
        ▼        ▼
   Return     Fetch from
   cached     server
   data       
        │        │
        │        ▼
        │   Update cache
        │        │
        └────┬───┘
             │
             ▼
   Component receives data
   + automatic re-render
```

### Mutation Flow (POST/PUT/DELETE)

```
1. Component calls mutation
   ↓
   const [createShipment] = useCreateShipmentMutation();
   await createShipment(data).unwrap();
   
2. RTK Query sends request
   ↓
   POST /shipments
   
3. Server responds
   ↓
   { id: '123', status: 'created' }
   
4. RTK Query invalidates tags
   ↓
   invalidatesTags: [{ type: 'Shipments', id: 'LIST' }]
   
5. Related queries refetch automatically
   ↓
   useGetShipmentsQuery() ← Refetches!
   
6. Components re-render with fresh data
   ↓
   UI updates automatically
```

---

## Cache Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         RTK Query Cache                             │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Shipments Cache                                             │   │
│  │  • Tag: 'Shipments:LIST'                                    │   │
│  │  • Data: [{ id: '1', ... }, { id: '2', ... }]             │   │
│  │  • Last Fetched: 2024-12-26 10:30:00                       │   │
│  │  • Status: 'fulfilled'                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Shipments By ID Cache                                       │   │
│  │  • Tag: 'Shipments:123'                                     │   │
│  │  • Data: { id: '123', trackingNumber: 'AMR123' }           │   │
│  │  • Last Fetched: 2024-12-26 10:31:15                       │   │
│  │  • Status: 'fulfilled'                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Partners Cache                                              │   │
│  │  • Tag: 'Partners:LIST'                                     │   │
│  │  • Data: [{ id: 'p1', ... }, { id: 'p2', ... }]           │   │
│  │  • Last Fetched: 2024-12-26 10:29:45                       │   │
│  │  • Status: 'fulfilled'                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Dashboard Stats Cache                                       │   │
│  │  • Tag: 'Dashboard'                                         │   │
│  │  • Data: { totalShipments: 150, activeShipments: 45 }      │   │
│  │  • Last Fetched: 2024-12-26 10:32:00                       │   │
│  │  • Status: 'fulfilled'                                      │   │
│  │  • Polling: Every 30 seconds                                │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Tag Invalidation System

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Tag Invalidation Flow                          │
└─────────────────────────────────────────────────────────────────────┘

User creates a new shipment:
   │
   ▼
createShipment({ origin: 'Mumbai', destination: 'Delhi' })
   │
   ▼
POST /shipments → Server creates shipment
   │
   ▼
RTK Query: invalidatesTags: [{ type: 'Shipments', id: 'LIST' }]
   │
   ▼
┌──────────────────────────────────────────────────────┐
│ All queries with 'Shipments:LIST' tag are marked    │
│ as stale and refetched automatically                 │
└──────────────────────────────────────────────────────┘
   │
   ├─────▶ useGetShipmentsQuery() refetches
   │
   ├─────▶ Dashboard stats refetches (has 'Dashboard' tag)
   │
   └─────▶ Components re-render with fresh data
   
Result: UI automatically shows new shipment! ✨
```

---

## Redux Store Structure

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Redux Store                                 │
│                                                                     │
│  {                                                                  │
│    auth: {                 ← Redux Slice (UI state)                │
│      user: { ... },                                                 │
│      token: 'jwt...',                                               │
│      isAuthenticated: true                                          │
│    },                                                               │
│                                                                     │
│    shipments: {            ← Redux Slice (UI state only)           │
│      selectedShipment: null,                                        │
│      filters: { status: 'all' }                                     │
│    },                                                               │
│                                                                     │
│    partners: {             ← Redux Slice (UI state only)           │
│      selectedPartner: null,                                         │
│      viewMode: 'grid'                                               │
│    },                                                               │
│                                                                     │
│    users: {                ← Redux Slice (UI state only)           │
│      selectedUsers: [],                                             │
│      filterRole: 'all'                                              │
│    },                                                               │
│                                                                     │
│    ui: {                   ← Redux Slice (UI preferences)          │
│      currentView: 'dashboard',                                      │
│      sidebarCollapsed: false,                                       │
│      theme: 'light',                                                │
│      notifications: []                                              │
│    },                                                               │
│                                                                     │
│    api: {                  ← RTK Query (Server data & cache)       │
│      queries: {                                                     │
│        'getShipments({"page":1})': {                               │
│          status: 'fulfilled',                                       │
│          data: [...],                                               │
│          endpointName: 'getShipments',                             │
│          requestId: 'abc123'                                        │
│        },                                                           │
│        'getPartners(undefined)': { ... },                          │
│        'getDashboardStats(undefined)': { ... }                     │
│      },                                                             │
│      mutations: {                                                   │
│        'createShipment': { ... }                                    │
│      },                                                             │
│      provided: {           ← Tag-based cache tracking              │
│        'Shipments': ['LIST', '123', '456'],                        │
│        'Partners': ['LIST'],                                        │
│        'Dashboard': []                                              │
│      },                                                             │
│      subscriptions: { ... },                                        │
│      config: { ... }                                                │
│    }                                                                │
│  }                                                                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Hook Generation

```
┌─────────────────────────────────────────────────────────────────────┐
│              How RTK Query Auto-generates Hooks                     │
└─────────────────────────────────────────────────────────────────────┘

API Slice Definition:
   │
   ▼
getShipments: builder.query<Response, Params>({
  query: (params) => ({ url: '/shipments', params }),
  providesTags: ['Shipments']
})
   │
   │ RTK Query automatically generates:
   │
   ├─────▶ useGetShipmentsQuery(params, options)
   │       • Returns: { data, isLoading, error, ... }
   │       • Auto-fetches on mount
   │
   ├─────▶ useLazyGetShipmentsQuery()
   │       • Returns: [trigger, result]
   │       • Manual control
   │
   └─────▶ useGetShipmentsQueryState(params)
           • Access query state without subscribing

Mutation Definition:
   │
   ▼
createShipment: builder.mutation<Response, Request>({
  query: (data) => ({
    url: '/shipments',
    method: 'POST',
    body: data
  }),
  invalidatesTags: ['Shipments']
})
   │
   │ RTK Query automatically generates:
   │
   └─────▶ useCreateShipmentMutation()
           • Returns: [mutate, { data, isLoading, error, ... }]
           • Call mutate(data) to trigger
```

---

## Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Authentication with RTK Query                    │
└─────────────────────────────────────────────────────────────────────┘

1. User logs in
   │
   ▼
const [login] = useLoginMutation();
await login({ email, password }).unwrap();
   │
   ▼
2. Server returns { token: 'jwt...', user: {...} }
   │
   ▼
3. Update Redux auth slice
   │
dispatch(setCredentials({ token, user }));
   │
   ▼
4. Token stored in Redux state
   │
   ▼
5. Base API prepareHeaders() reads token
   │
prepareHeaders: (headers, { getState }) => {
  const token = getState().auth.token;
  if (token) {
    headers.set('Authorization', `Bearer ${token}`);
  }
}
   │
   ▼
6. All subsequent API calls include token automatically
   │
GET /shipments
Headers: { Authorization: 'Bearer jwt...' }
   │
   ▼
7. If server returns 401 (Unauthorized)
   │
baseQueryWithReauth catches 401 → dispatch(logout())
   │
   ▼
8. User redirected to login page
```

---

## Performance Optimization

```
┌─────────────────────────────────────────────────────────────────────┐
│                  RTK Query Performance Features                     │
└─────────────────────────────────────────────────────────────────────┘

1. Request Deduplication
   ┌─────────────────────────────────────────────────────┐
   │ Multiple components call useGetShipmentsQuery()     │
   │ → Only ONE actual HTTP request is made             │
   │ → All components receive the same cached data      │
   └─────────────────────────────────────────────────────┘

2. Automatic Garbage Collection
   ┌─────────────────────────────────────────────────────┐
   │ Query not used for 60s → automatically removed     │
   │ Saves memory and keeps cache clean                 │
   └─────────────────────────────────────────────────────┘

3. Background Refetching
   ┌─────────────────────────────────────────────────────┐
   │ User navigates away → data stays cached            │
   │ User returns → shows cached data + refetches       │
   │ Result: Instant display + fresh data               │
   └─────────────────────────────────────────────────────┘

4. Normalized Cache
   ┌─────────────────────────────────────────────────────┐
   │ Same shipment in multiple queries → stored once    │
   │ Update in one place → updates everywhere           │
   └─────────────────────────────────────────────────────┘

5. Selective Re-rendering
   ┌─────────────────────────────────────────────────────┐
   │ Only components using affected data re-render       │
   │ Other components stay unchanged                     │
   └─────────────────────────────────────────────────────┘
```

---

## 🎯 Summary

This architecture provides:

✅ **Automatic caching** - Smart data storage and retrieval  
✅ **Tag-based invalidation** - Keep data fresh automatically  
✅ **Type-safe** - Full TypeScript support  
✅ **Performance optimized** - Request deduplication & caching  
✅ **Developer friendly** - Less code, better DX  
✅ **User friendly** - Faster UI, better UX  

**Result: Production-ready data fetching solution! 🚀**
